# External release gate

Status on 2026-09-25: **NOT RELEASE READY**. Every check is `PASS`, `FAIL`, `BLOCKED`, or `NOT APPLICABLE`. `BLOCKED` identifies a missing input or environment; `FAIL` is an observed defect. Keep direct video URL import disabled until its actual-connection security policy is implemented and tested.

| Area | Gate | Status | Evidence / action to clear |
| --- | --- | --- | --- |
| Security | OpenRouter secret isolated in Keychain and sanitized gateway | PASS | Source audit and mocked gateway/vault tests passed; never collect an unredacted full process dump. |
| Security | Direct URL SSRF/DNS-rebinding exposure in external build | PASS | Compile-time gate rejects before DNS/network; option hidden. Dormant downloader is unsafe to re-enable. |
| Security | Enabled direct URL redirect/DNS-rebinding transport | NOT APPLICABLE | Direct URL feature is disabled. Require actual-peer validation and adversarial tests before enabling. |
| Security | No high-severity finding reachable in distributed build | BLOCKED | Re-audit the Developer ID-signed build and installed `yt-dlp`/media decoder boundary. |
| Functionality | Complete local import → preview → edit → export → relaunch workflow | BLOCKED | A silent-media pipeline test reached final render, but interactive edit/export/relaunch and speech workflow remain unverified. See [QA_RESULTS.md](QA_RESULTS.md). |
| Functionality | On-device speech transcription on real long media | PASS | QA-LECTURE-1080 completed 32:56 with 1,022 timed words after the authorization callback and chunk-boundary fixes; word accuracy still requires human review. |
| Functionality | Authorized YouTube import and failure UX | PASS | 2026-09-25, no FFmpeg or Homebrew yt-dlp: the in-app installer verified yt-dlp 2026.08.19 by SHA-256. `YouTubeEndToEndTests` (opt-in, live `z-ai/glm-5.3-flash`) turned NASA's public-domain *Astronaut Talks About Life and Work on the Space Station* (12:30, 720p) into 3 captioned 1080×1920 clips (download 125 s, processing 387 s, peak RSS 172 MB), and the CC BY *Sprite Fright* (10:30) into 1 labeled best-available clip (processing 147 s, peak RSS 120 MB). Failure text maps to fixed messages. The packaged-app Home flow still needs one interactive run. |
| Quality | Representative real-video moment, framing, pacing, caption review | BLOCKED | Four licensed/public-domain sources are recorded in [QA_ASSETS.md](QA_ASSETS.md). Full scoring and speech, screen/demo, caption, crop and pacing inspection remain open. |
| Quality | 1080p and 4K final files inspected in independent player | BLOCKED | Two real 4K segments and one silent vertical file passed probe/full decode and still-frame inspection; independent player and complete workflow review remain open. |
| Performance | Long-form 1080p CPU/GPU/RSS/disk/stage profile | BLOCKED | 33-minute local analysis and transcription measurements exist, but complete app CPU/GPU/RSS/disk/stage profile does not. See [PERFORMANCE_RESULTS.md](PERFORMANCE_RESULTS.md). |
| Performance | Long-form 4K CPU/GPU/RSS/disk/stage profile | BLOCKED | 59-minute 4K analysis/proxy and no-speech coordinator measurements exist; full app CPU/GPU/RSS/disk/stage profile with the production speech/model path does not. |
| Reliability | Automated cancellation, save, interrupted-render cleanup, export-capacity, network-error regressions | PASS | Full suite rerun on 2026-09-25: 112 tests, 106 passed, six opt-in/benchmark skipped, zero failures. Startup cleanup test preserves accepted and unrelated files and skips invalid manifests and symlinked Exports. |
| Reliability | Controlled final-render disk-full cleanup | PASS | Disposable 64 MiB APFS volume forced a real 4K export failure; renderer returned a safe error and removed incomplete MP4 files. Other disk-full stages remain blocked. |
| Reliability | Live force quit, disk full, network interruption and recovery | BLOCKED | Cleared sources exist; controlled interactive interruption runs still required. See [RELIABILITY_RESULTS.md](RELIABILITY_RESULTS.md). |
| Distribution | Developer ID-signed Release build with hardened runtime and timestamp | PASS | Rebuilt after interrupted-render cleanup on 2026-09-25; unrestricted `codesign --verify --deep --strict` passed, Team ID `8QSM298XJ2`, runtime flag and secure timestamp present. The earlier pre-notarization ZIP extracted with a valid signature; a new distribution ZIP has not been made. |
| Distribution | Notarization Keychain profile available | BLOCKED | Rechecked on 2026-09-25: `xcrun notarytool history --keychain-profile cliphelm-release` found no item. Supply an existing profile name or store credentials in Keychain; do not put secrets in Git or arguments. |
| Distribution | Notarization accepted and ticket stapled | BLOCKED | [DISTRIBUTION.md](DISTRIBUTION.md) and `scripts/distribute-app.sh` prepare the path; submit using the Keychain notary profile, require Accepted status, staple and validate. |
| Distribution | Current signed candidate passes Gatekeeper | FAIL | Rechecked after the 2026-09-25 signed rebuild: `spctl -a -t exec -vv` rejected it as `Unnotarized Developer ID`. Retest after notarization and stapling. |
| Distribution | Stapling, Gatekeeper, clean install and distributed-build workflow | BLOCKED | Run on final accepted notarized artifact in clean environment; test Keychain, media access, OpenRouter, render, export, relaunch. YouTube also requires the documented `yt-dlp` prerequisite. |

The detailed synthetic baseline remains in [QUALITY_BENCHMARK.md](QUALITY_BENCHMARK.md). It does not satisfy the quality or performance gates above.
